# 页面定位（统一前提）

**页面类型**：逻辑视图 · 语义理解详情页
**页面目标**：

> 在「不脱离逻辑视图」的前提下，完成
> **字段语义理解 → 业务对象识别 → 关系呈现 → 治理判断**

---

# 一、页面整体 Frame 结构（最外层）

```text
Frame: LogicViewSemanticDetailPage
├─ PageHeader
├─ SemanticAssistBar
├─ SemanticProgressBanner
├─ TabNavigation
└─ TabContentArea
```

---

# 二、顶部区域（强复用 + 小调整）

## 1️⃣ PageHeader（复用 + 状态语义统一）

```text
Component: PageHeader
├─ BackButton
├─ ViewTitleGroup
│   ├─ LogicViewName
│   ├─ DataSourceTag
│   └─ ObjectTypeTag（主体 / 过程 / 状态）
├─ SemanticStatusBadge（新）
└─ PrimaryCTAButton
```

### SemanticStatusBadge（新增统一语义）

```text
未开始语义建模
字段语义待确认
语义建模进行中
可进入对象建模
```

> ✅ 替换现在的「AI 建议中 / 已分析」等功能态
> ❌ 不再出现系统状态词

---

## 2️⃣ SemanticAssistBar（轻调整，保留你已有）

```text
Component: SemanticAssistBar
├─ Toggle: 语义理解辅助检测（On/Off）
├─ TemplateSelector（SEMANTIC_MIN）
├─ SamplingRatio
├─ SignalStatus
└─ SettingsIcon
```

> 这是**系统能力区**，不属于用户决策区

---

## 3️⃣ SemanticProgressBanner（整合你现在散落的信息）

```text
Component: SemanticProgressBanner
├─ InfoText（当前仅展示字段结构与统计特征）
└─ ProgressHint（语义理解完成后生成业务语义建议）
```

---

# 三、Tab 导航（保持你现在的 4 个）

```text
Component: TabNavigation
├─ Tab: 业务理解
├─ Tab: 字段与属性
├─ Tab: 对象关系
└─ Tab: 治理与风险
```

---

# 四、Tab ① 业务理解（重点改造）

## 4.1 业务理解 Tab Frame

```text
Frame: Tab_BusinessUnderstanding
├─ ObjectSummaryCard
├─ ConfidenceImproveCard
├─ EvidenceCard
└─ DecisionActionBar
```

---

### ObjectSummaryCard（来自你现在「综合语义结论」重构）

```text
Component: ObjectSummaryCard
├─ Header
│   ├─ Title: 业务对象理解结论
│   └─ ConfidenceBadge（0% / 需复核）
├─ ObjectIdentityForm
│   ├─ BusinessObjectName（Editable）
│   ├─ BusinessDomain
│   ├─ DataLayer
│   └─ ObjectTypeSelector（主体 / 过程 / 状态 / 清单）
└─ DataGranularityInput
```

📌 **注意**

* 不叫「综合语义结论」
* 不强调 AI
* 强调「这是对象结论」

---

### ConfidenceImproveCard（你现有 AI 建议卡片结构化）

```text
Component: ConfidenceImproveCard
├─ Header
│   ├─ Title: 置信度提升建议
│   └─ CurrentConfidenceValue
├─ ImproveActionTable
│   ├─ FactorName
│   ├─ StatusBadge
│   ├─ SuggestedAction
│   └─ ExpectedImprove
└─ HintText
```

---

### EvidenceCard（你已有“证据”Tab 内收）

```text
Component: EvidenceCard
├─ Header: 理解依据
├─ EvidenceGroup
│   ├─ EvidenceTypeTag（命名 / 分布 / 规则）
│   ├─ EvidenceDescription
│   └─ RelatedFieldLink
```

---

### DecisionActionBar（统一裁决）

```text
Component: DecisionActionBar
├─ InfoText（系统建议，需人工确认）
├─ Button: 确认理解结果
└─ Button: 暂不处理
```

---

# 五、Tab ② 字段与属性（最关键）

## 5.1 Tab Frame

```text
Frame: Tab_FieldAndAttribute
├─ FieldStatsBar
├─ FieldAttributeTable
```

---

### FieldStatsBar（你现在的统计卡片合并）

```text
Component: FieldStatsBar
├─ TotalFieldCount
├─ KeyFieldCount
├─ BusinessAttributeCount
└─ RelationFieldCount
```

---

### FieldAttributeTable（在你现有字段表上扩展）

```text
Component: FieldAttributeTable
├─ TableHeader
├─ TableRow (repeat)
│   ├─ PhysicalFieldName
│   ├─ BusinessMeaning (Editable)
│   ├─ MappedBusinessObject
│   ├─ FieldRoleTag
│   │   ├─ 主键
│   │   ├─ 业务属性
│   │   ├─ 对象关联
│   │   └─ 描述字段
│   ├─ AIConfidenceIndicator
│   └─ IssueIndicator
└─ RowExpandableArea
    ├─ SampleValues
    ├─ QualitySignals
    └─ RuleHits
```

📌 Field Role

* **Tag 展示**
* 非必填
* 不弹窗、不强制

---

# 六、Tab ③ 对象关系（弱但必须）

## 6.1 Tab Frame

```text
Frame: Tab_ObjectRelation
├─ RelationLegend
├─ ObjectRelationGraph
└─ RelationDetailSidePanel
```

---

### ObjectRelationGraph

```text
Component: ObjectRelationGraph
├─ Node: BusinessObject
│   ├─ ObjectName
│   └─ ObjectTypeTag
└─ Edge: ObjectRelation
    ├─ RelationType
    └─ StatusTag（建议 / 已确认）
```

---

### RelationDetailSidePanel

```text
Component: RelationDetailSidePanel
├─ RelationSummary
├─ SourceFields
└─ HintText（后续在业务对象建模中确认）
```

---

# 七、Tab ④ 治理与风险（复用你现有能力）

## 7.1 Tab Frame

```text
Frame: Tab_GovernanceAndRisk
├─ GovernanceSummaryCards
├─ QualitySection
├─ SecuritySection
└─ RiskItemTable
```

---

### GovernanceSummaryCards

```text
Component: GovernanceSummaryCards
├─ GovernanceStatus
├─ GateStatus
├─ RiskCount
└─ PendingReviewCount
```

---

### QualitySection / SecuritySection

```text
Component: QualitySection
├─ Completeness
├─ KeyMissing
└─ QualityHints

Component: SecuritySection
├─ SensitivityLevel
├─ PIIInfo
└─ AccessSuggestion
```

---

# 八、你现在「不需要做」但容易被误加的

❌ 不新增字段治理页面
❌ 不新增术语 / 标签入口
❌ 不在此页做业务对象编辑
❌ 不让用户“配置模型规则”

---

